import 'package:flutter/material.dart';

class Ball {
  final double x, y;
  final Color color;
  final int id;
  const Ball({required this.x, required this.y, required this.color, required this.id});
}
